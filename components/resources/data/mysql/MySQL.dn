data MySQLField {
	char name[]
	char value[]
}

data MySQLRow {
	MySQLField fields[]
}

interface MySQL {
	MySQL(char host[], int port, char user[], char pass[], char dbName[])
	bool execute(char query[])
	void close()
	MySQLRow[] fetchAll()
}